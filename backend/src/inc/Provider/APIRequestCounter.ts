/**
 * Helper for check API Max Requests
 */
export class APIRequestCounter {

    public static async isDayMaxRequest(name: string, maxRequests: number): Promise<boolean> {
        return false;
    }

}